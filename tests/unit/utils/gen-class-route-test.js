import genClassRoute from 'frontend-metis/utils/gen-class-route';
import { module, test } from 'qunit';

module('Unit | Utility | gen-class-route', function() {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = genClassRoute();
    assert.ok(result);
  });
});
