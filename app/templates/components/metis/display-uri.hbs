<a href={{@uri}}>
  {{#if this.hasShort}}
    {{this.label}}
  {{else}}
    {{@uri}}
  {{/if}}
</a>

<button {{on "click" (fn (mut this.showShort) (not this.showShort))}}>
  {{#if this.showShort}}
    [+]
  {{else}}
    [-]
  {{/if}}
</button>
